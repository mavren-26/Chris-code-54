🔹 Problem

Given an unsorted array of integers nums, return the length of the longest consecutive elements sequence.

🔹 Key idea (Hashing)

Store all numbers in a hash set and only start counting when a number has no previous consecutive number.

🧠 Logic

Only start sequence from numbers where:

num - 1 does NOT exist